{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Domain",
    instructions: "The domain to use for shortening URLs.",
    id: 'domain',
    name: 'domain',
    suggestEnvVars: true,
    value: settings.domain,
    errors: settings.getErrors('domain'),
    required: true,
    placeholder: "example.com"
}) }}
